<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.penyo.herbms.mapper.ExperienceMapper">

  <resultMap id="ExperienceBean" type="ExperienceBean">
    <id property="herbId" column="herb_id"/>
  </resultMap>

  <insert id="add" parameterType="ExperienceBean">
    INSERT experiences (herb_id, derivation, content)
    VALUES (#{herbId}, #{derivation}, #{content})
  </insert>

  <delete id="delete" parameterType="int">
    DELETE
    FROM experiences
    WHERE id = #{id}
  </delete>

  <update id="update" parameterType="ExperienceBean">
    UPDATE experiences
    SET herb_id    = #{herbId},
        derivation = #{derivation},
        content    = #{content}
    WHERE id = #{id}
  </update>

  <select id="selectById" resultMap="ExperienceBean" parameterType="int">
    SELECT *
    FROM experiences
    WHERE id = #{id}
  </select>

  <select id="selectByFields" resultMap="ExperienceBean" parameterType="java.util.List">
    SELECT * FROM experiences
    WHERE
    <foreach collection="list" item="item" separator="AND">
      CONCAT_WS(derivation, content)
      LIKE CONCAT('%', #{item}, '%')
    </foreach>
  </select>

  <select id="selectAll" resultMap="ExperienceBean">
    SELECT *
    FROM experiences
  </select>

</mapper>